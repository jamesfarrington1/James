function setup() {
    createCanvas(windowWidth, windowHeight);
}

function draw() {
    background('black');
    var y = windowHeight / 2;

    CSSNumericValue(x ,y, 200);


    if (x > windowWidth) {
        

}




function windowResized() {
    resizeCanvas(windowWidth, windowHeight);
}